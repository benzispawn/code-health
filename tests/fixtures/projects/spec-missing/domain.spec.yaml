project:
  name: email-saas
  framework: nestjs
domains:
  - name: billing
    path: src/billing
    controllers:
      - name: BillingController
        routes:
          - method: POST
            path: /change-plan
            handler: changePlan
    decorators:
      - name: MissingDecorator
